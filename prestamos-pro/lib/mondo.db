import { MongoClient } from "mongodb";

const uri = process.env.MONGO_URL;

const client = new MongoClient(uri);

export async function db() {
  await client.connect();
  return client.db("prestamos");
}